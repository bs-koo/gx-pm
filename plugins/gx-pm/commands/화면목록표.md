---
description: "DE-03 화면목록표를 생성합니다. 요구사항 기반으로 화면을 식별하고 기능구분/화면ID를 부여. | 자연어: 화면목록 만들어줘, 화면 설계, 화면 뽑아줘, 화면목록표 작성"
argument-hint: "<요구사항정의서 또는 파일>"
---

# /화면목록표 — DE-03 화면목록표 생성

> **공통 규칙**: `templates/approval-protocol.md`의 승인 루프 프로토콜을 적용한다.

## 워크플로우

### Step 1: 요구사항 로드

- 이전 `/요구사항정의서` 결과가 있으면 자동 로드
- 없으면 요구사항정의서 입력을 요청

### Step 2: 시안/대안 감지 [필수 중단점]

**detect-alternatives** 스킬 적용. 화면 구성 방식에 대안이 있으면 선택 요청:
- 단일 화면 vs 별도 메뉴
- 탭 구성 vs 페이지 분리

선택 전까지 다음 단계로 진행하지 않는다.

### Step 3: 화면목록표 생성

**generate-screen-list** 스킬 적용:
- 기능구분ID, 기능ID 부여
- 화면ID 자동 계산 ({기능ID}_{SN})
- 망구분 판별 (프로젝트 망 유형 기반)

### Step 4: 화면목록 검토 [필수 중단점 — 승인 루프]

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 화면 {N}개 구성을 확인해주세요

주요 확인:
1. 같은 페이지 영역이 별도 화면으로 분리되지 않았는지
2. 기능구분/기능 분류가 실제 업무와 일치하는지
3. 망구분(내부/외부)이 올바른지
4. 누락된 화면이 있는지

✅ 승인 — "승인" 또는 "OK"
✏️ 수정 — 변경 내용 입력

  수정 예시:
  • "SC-003, 004, 005는 한 화면이야. 합쳐줘"
  • "망구분 전부 내부망으로"
  • "팝업 화면 2개 추가"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

사용자가 승인할 때까지 수정 → 재출력 → 승인 요청을 반복한다.

### Step 5: 결과 저장 + xlsx 추출

파일명: `{시스템코드}-화면목록표.md`

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 xlsx로 추출할까요?
  1. xlsx 추출
  2. 마크다운만 저장
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Step 6: 다음 제안

- "/프로그램정의서 로 프로그램 구조를 정의할까요?"
- "/인터페이스정의서 로 외부 연동을 정의할까요?"
- "/단위테스트계획서 로 테스트를 계획할까요?"
