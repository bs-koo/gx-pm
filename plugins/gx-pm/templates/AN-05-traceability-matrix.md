# AN-05 추적매트릭스 양식

## 파일 형식
Excel (.xlsx)

## 시트 구성
1. **개정이력** — 규칙의 정본은 `templates/revision-history.md`
2. **추적매트릭스** — 본문 데이터

## 이 문서는 대조기다

앞의 네 산출물을 읽어 **끊긴 곳을 찾는 것**이 본업이고, 표는 그 부산물이다.
새 정보를 만들지 않는다.

## 본문 컬럼 (정본)

| # | 컬럼 | 출처 |
|---|------|------|
| 1 | 요구사항ID | AN-02 |
| 2 | 요구사항명 | AN-02 |
| 3 | 상태 | AN-02 를 그대로 옮긴다. 값 정본은 `templates/AN-02-requirements-definition.md` 「상태 판정」 — 여기서 3값을 복제하지 않는다 |
| 4 | 기능ID | AN-03 `연계요구사항ID` 역조회 |
| 5 | 기능명 | AN-03 |
| 6 | 테이블·컬럼 | DE-08 `연계기능ID` 역조회. `TB_SCORE (신규 4)` 형태 |
| 7 | 테스트 수 | DE-13 `연계기능ID`·`연계요구사항ID` 역조회 |
| 8 | Pass/Fail | DE-13 `결과` 집계. 테스트 수 0건이면 `—`, 테스트는 있는데 `결과`가 전건 공란이면 `0/0`(사유는 `누락` 열 참조) |
| 9 | 누락 | 아래 누락 판정표 |

## 누락 판정

| 유형 | 판정 조건 | 표기 |
|------|----------|------|
| 기능 미도출 | 기능 요구사항인데 연계된 기능이 0건 | `기능 미도출` |
| 테스트 미작성 | 기능이 있는데 테스트가 0건 | `테스트 미작성` |
| 비기능 검증 누락 | 어느 기능에도 안 걸린 요구사항인데 비기능 테스트도 0건 | `비기능 검증 누락` |
| 정상만 검증 | 그 기능의 테스트가 전부 정상 흐름 | `예외 케이스 없음` |
| 미수행 | 테스트는 있는데 `결과` 가 전건 공란 | `미수행` |
| 실패 잔존 | `Fail` 이 1건 이상 | `실패 {N}건` |
| 삭제 미정리 | 상태가 `삭제` 인데 연계된 기능·테스트가 살아 있음 | `삭제 미정리` |
| 설계 제약 미반영 | 데이터 요구사항인데 DE-08 어느 컬럼의 `근거` 열에도 그 요구사항ID 가 없음 | `설계 제약 미반영` |
| 임시확정 | 3차까지 요청했으나 미응답이라 관행값을 채운 항목이 이 요구사항에 걸려 있음 | `임시확정` |

**비기능 요구사항에 기능ID 가 없는 것은 누락이 아니다.** DE-13 의 `연계기능ID` 공란
행으로 검증되면 정상이다.

**데이터 요구사항의 정의와 반영 절차는 `skills/convert-ddl-to-tablespec/SKILL.md`
Step 3 이 정본이다.** 여기서 다시 정의하지 않는다 — 기능을 거치지 않고 데이터의
구성·규칙·제약만 규정하는 요구사항(회차 누적, 이력 보존 등)이라는 것만 표시한다.

**`임시확정` 은 결함이 아니라 위험 표시다.** 값이 채워져 있어 다른 판정에 걸리지
않으므로 따로 자리를 준다. 전환 규칙은 `templates/evidence-rules.md`
§[임시확정] 은 3차 미응답의 결과다 가, 요청 이력은
`templates/confirmation-request.md` 의 「요청 이력」 시트가 정본이다.

## 예시 행

| 요구사항ID | 요구사항명 | 상태 | 기능ID | 기능명 | 테이블·컬럼 | 테스트 수 | Pass/Fail | 누락 |
|---|---|---|---|---|---|---|---|---|
| REQ-001 | 벤치마크 스코어테이블 생성 기능 | 유지 | FN-001 | 벤치마크 스코어테이블 생성 | TB_SCORE (신규 4) | 6 | 6/0 | |
| REQ-045 | 동시접속 500명 응답 3초 | 유지 | | | | 2 | 0/0 | 미수행 |
| REQ-032 | 업로드 통계 데이터 조회 | 유지 | | | | 0 | — | 기능 미도출 |

## 작성 규칙

- 요구사항 1건이 기능 여러 개에 걸리면 행을 나누고 요구사항ID 를 세로 병합한다
- 이 표는 손으로 채우지 않는다. 네 산출물에서 기계적으로 조립한다
- 개정이력 기입은 `skills/manage-revision-history` 가 담당한다
