# gx-pm — 공공/SI PM 플러그인

## 이 플러그인은

공공/SI 프로젝트의 PM(프로젝트 매니저)을 위한 Claude Cowork 플러그인입니다.
실제 공공 프로젝트 산출물 양식(요구사항정의서, 화면목록표, 테이블정의서, 테스트계획서 등)에 맞는 데이터를 자동 생성하고, 산출물 간 ID 추적 체계를 관리합니다.

## 핵심 개념

### ID 추적 체인
모든 산출물은 하나의 ID 체인으로 연결됩니다:
```
제안요청ID (SFR-xxx)
  → 요구사항ID ({시스템코드}-RE-xxx)
    → 화면ID ({접두}_{xx}_{xx}_{xxx})
      → 프로그램ID (PG_{화면ID})
        → 단위테스트ID (U_{화면ID})
          → 통합테스트ID ({시스템코드}-TE-xxx)
```

이 체인이 끊기지 않도록 관리하는 것이 감리 대응의 핵심입니다.
`/pm-trace` 커맨드로 언제든 추적 상태를 확인하고 누락을 탐지할 수 있습니다.

### 시스템 코드
프로젝트의 각 시스템은 영문 1자리 코드(A~I)를 가집니다.
이 코드는 요구사항ID, 통합테스트ID의 접두어로 사용됩니다.

## 시작하기

1. 플러그인 설치 후 `/pm-analyze`로 요구사항 분석 시작
2. `/pm-design`으로 화면목록표 + 프로그램정의서 생성
3. `/pm-table`로 ERDCloud DDL ↔ 테이블정의서 변환
4. `/pm-test`로 단위/통합 테스트 계획서 생성
5. `/pm-trace`로 전체 추적매트릭스 확인

## 커맨드 목록

| 커맨드 | 설명 |
|--------|------|
| `/pm-analyze` | RFP → 요구사항 추출 → 분류 → 우선순위 |
| `/pm-design` | 요구사항 → 화면목록 + 프로그램정의서 + 인터페이스 |
| `/pm-table` | ERDCloud DDL ↔ 테이블정의서 |
| `/pm-test` | 단위테스트계획 + 통합테스트시나리오 |
| `/pm-trace` | ID 추적 + 누락 탐지 |
| `/pm-audit` | 감리 대응 문서 + 증빙 체크리스트 |
| `/pm-result` | 테스트 결과서 기입 (IM-03, TE-02, TE-06) |
| `/pm-report` | 회의록 또는 주간보고서 |

## 산출물 범위

이 플러그인이 지원하는 산출물:
- AN-02 요구사항정의서
- AN-05 요구사항추적매트릭스
- DE-03 화면목록표
- DE-05 프로그램정의서
- DE-08 테이블정의서
- DE-13 단위테스트계획서 / IM-03 결과서
- DE-14 통합테스트계획서 / TE-02 결과서 / TE-06 인수테스트결과서

## 주의사항

- 모든 프로그램ID는 `PG_{화면ID}` 형식을 따릅니다
- 시스템코드는 영문 대문자 1자리 (A~I)
- 비기능 요구사항은 화면/프로그램 매핑 대상이 아닙니다
- 템플릿은 `templates/` 폴더에서 양식 상세를 확인할 수 있습니다
