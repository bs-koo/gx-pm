---
description: "DE-13 단위테스트계획서를 생성합니다. 화면목록표 기반으로 화면유형 판별 후 검사기준 자동 매핑. | 자연어: 단위테스트 계획, 단위테스트계획서 만들어줘, 단테 계획, 화면별 테스트"
argument-hint: "<화면목록표 또는 파일>"
---

# /단위테스트계획서 — DE-13 단위테스트계획서 생성

> **공통 규칙**: `templates/approval-protocol.md`의 승인 루프 프로토콜을 적용한다.

## 워크플로우

### Step 1: 화면목록표 로드

- 이전 `/화면목록표` 결과가 있으면 자동 로드
- 없으면 화면목록표 입력 요청

### Step 2: 화면 유형 판별

**generate-unit-test-plan** 스킬의 유형 판별: 목록형, 등록형, 수정형, 상세형, CRUD통합형, API형

### Step 3: 화면 유형 검토 [필수 중단점 — 승인 루프]

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 화면 유형 판별 결과를 확인해주세요

| 화면ID | 화면명 | 판별 유형 |
|--------|--------|----------|

✅ 승인 — "승인" 또는 "OK"
✏️ 수정 — 변경 내용 입력

  수정 예시:
  • "SC-003은 CRUD통합형이 아니라 입력+조회형"
  • "SC-010은 API형으로 변경"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

사용자가 승인할 때까지 수정 → 재출력 → 승인 요청을 반복한다.

### Step 4: 단위테스트 항목 생성

화면 유형 기반 검사기준 자동 매핑, 단위테스트ID 부여 (U_{화면ID}).

### Step 5: 테스트 항목 검토 [필수 중단점 — 승인 루프]

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 단위테스트 항목 {N}건을 확인해주세요

✅ 승인 — "승인" 또는 "OK"
✏️ 수정 — 변경 내용 입력

  수정 예시:
  • "SC-001에 '빈 값 입력 시 기본값 적용' 테스트 추가"
  • "경계값 테스트를 더 추가해줘"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

사용자가 승인할 때까지 수정 → 재출력 → 승인 요청을 반복한다.

### Step 6: 결과 저장 + xlsx 추출

파일명: `{시스템코드}-단위테스트계획서.md`

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 xlsx로 추출할까요?
  1. xlsx 추출  2. 마크다운만 저장
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Step 7: 다음 제안

- "/통합테스트시나리오 로 E2E 시나리오를 만들까요?"
- "/추적매트릭스 로 테스트 커버리지를 확인할까요?"
